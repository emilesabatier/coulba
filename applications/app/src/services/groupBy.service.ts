
// export function groupBy<T extends { [key: string]: unknown }>(data: T[], key: string) {
//     if (data.length === 0) []
//     const result = groupBy(data, (object: T) => object[key])

//     return data?.reduce((previous, object) => {
//         previous.push(item)
//         return previous
//     }, [])

// }
