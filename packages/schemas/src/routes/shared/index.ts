import { companies } from "./companies"
import { sessions } from "./sessions"
import { users } from "./users"


export const shared = {
    companies: companies,
    sessions: sessions,
    users: users
}
